#include <stdio.h>

/**
 * main - entry point
 * @argc: the ammount of items in argv
 * @argv: an array which contains all the arguments passed
 * through the compiler
 *
 * Description: prints the number of arguments passed into main,
 * followed by a newline
 *
 * Return: 0
 */
int main(int argc, __unused__((argument))char *argv[])
{
	printf("%d\n", argc);

	return (0);

}
