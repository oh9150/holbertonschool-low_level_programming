#include "main.h"

/**
 * _isdigit - check description
 * @c: the value to check.
 *
 * Description: checks if c is a digit(number).
 *
 * Return: 1 if digit, 0 otherwise.
 */
int _isdigit(int c)
{
	int i;

	for (i = 0; i <= 9; i++)
		return (1);
	return(0);
